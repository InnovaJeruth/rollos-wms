node_modules/

# Datos operativos — escritos por la app vía GitHub API, no por git
inventario/actual.json
pendientes/*.json
auditoria/*.json
procesados/*.json
logs/*.json

# Bot Python — archivos generados / sensibles
.env
__pycache__/
*.pyc
logs/
inventario_descargado/
INVENTARIO_*.txt
